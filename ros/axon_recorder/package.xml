<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>axon_recorder</name>
  <version>0.1.0</version>
  <description>
    Axon data recorder for ROS.
    Records ROS topic data to MCAP format for efficient storage and playback.
    Supports both ROS 1 (Noetic) and ROS 2 (Humble/Jazzy/Rolling).
  </description>
  
  <maintainer email="maintainer@archebase.com">ArcheBase</maintainer>
  <license>Apache-2.0</license>
  
  <url type="website">https://github.com/archebase/axon</url>
  <url type="repository">https://github.com/archebase/axon</url>
  <url type="bugtracker">https://github.com/archebase/axon/issues</url>
  
  <author email="maintainer@archebase.com">ArcheBase</author>

  <!-- Build tool dependency -->
  <buildtool_depend condition="$ROS_VERSION == 1">catkin</buildtool_depend>
  <buildtool_depend condition="$ROS_VERSION == 2">ament_cmake</buildtool_depend>

  <!-- ROS 1 dependencies -->
  <depend condition="$ROS_VERSION == 1">roscpp</depend>
  <depend condition="$ROS_VERSION == 1">std_msgs</depend>
  <depend condition="$ROS_VERSION == 1">sensor_msgs</depend>
  <depend condition="$ROS_VERSION == 1">nav_msgs</depend>
  <depend condition="$ROS_VERSION == 1">geometry_msgs</depend>
  <build_depend condition="$ROS_VERSION == 1">message_generation</build_depend>
  <exec_depend condition="$ROS_VERSION == 1">message_runtime</exec_depend>

  <!-- ROS 2 dependencies -->
  <depend condition="$ROS_VERSION == 2">rclcpp</depend>
  <depend condition="$ROS_VERSION == 2">std_msgs</depend>
  <depend condition="$ROS_VERSION == 2">sensor_msgs</depend>
  <depend condition="$ROS_VERSION == 2">nav_msgs</depend>
  <depend condition="$ROS_VERSION == 2">geometry_msgs</depend>
  <buildtool_depend condition="$ROS_VERSION == 2">rosidl_default_generators</buildtool_depend>
  <exec_depend condition="$ROS_VERSION == 2">rosidl_default_runtime</exec_depend>

  <!-- Common dependencies -->

  <!-- Member of group - MUST be direct child of <package>, NOT inside <export> -->
  <!-- This is required for packages that generate ROS 2 service/message interfaces -->
  <member_of_group>rosidl_interface_packages</member_of_group>

  <!-- Export section -->
  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
